# 隐私与发布范围

## 这个公开仓库包含什么

仓库只包含模组源码、安装说明、许可证、第三方署名、Git 配置规则和模拟展示图。首次发布使用独立目录和全新的 Git 历史，不包含开发工作区及其旧文件历史。

不发布：

- 真实桌面 / 任务栏截图、剪贴板原图或音视频；
- 本机用户名、私人目录、个人邮箱、凭证和密钥；
- 真实硬件测试结果、日志、内存转储或性能历史；
- 编译后的 DLL / EXE、第三方二进制或本地构建脚本。

`.gitignore` 使用文件允许列表，新增文件需要显式调整规则。展示图由隔离的原生组件测试宿主配合模拟数值生成，使用示意背景，不含用户真实截图或实测数据。提交署名使用 GitHub 的 noreply 隐私邮箱；公开 GitHub 用户名保留为作者署名。

## 模组运行时读取什么

- 系统汇总资源指标及进程 / 线程 / 句柄的**数量**。
- 物理显卡型号、适配器标识及驱动提供的资源 / 传感器读数。
- 系统盘盘符、总容量和剩余容量。
- 可选的本机 LibreHardwareMonitor 温度接口。
- Explorer 自身的任务栏结构，用于附加原生控件。

最值仅统计滚动一小时内的有效样本，并仅存在于进程内存中，不写入历史文件；到期样本自动移出。读取不到的参数不会以零值冒充正常读数。

模组不读取聊天记录、提示词、终端内容、浏览历史或个人文件内容。它只为查找可选传感器提供程序而检查相应进程是否运行，不收集运行中应用的窗口标题或命令行。

## 网络与日志

模组自身的 HTTP 客户端仅访问 `127.0.0.1:<配置端口>/data.json`，不使用代理、重定向或自动认证；没有遥测或向云端上传资源读数的代码。源码中的 XML 命名空间 URL 只是格式标识，不是网络请求。

Windhawk 主程序可能按其自身配置下载编译器、模组或调试符号；这些行为不属于本模组的 HTTP 客户端。LibreHardwareMonitor 的服务监听范围应由用户限制，不应向公网开放。

故障会向 Windhawk 日志记录运行状态或系统错误。分享日志前仍应自行检查；不要公开完整诊断转储或系统记录。

## 报告问题前

优先提供最小复现步骤、软件版本与已脱敏的错误描述。截图仅保留组件区域，并检查是否包含私人数据；不要上传账户信息、工作路径、设备序列号、密码、令牌或其他秘密。

首次公开发布经过文件允许列表、常见凭证模式、私人路径和提交邮箱检查。这是一项发布审查，不是对所有未来提交或运行环境的安全保证。
